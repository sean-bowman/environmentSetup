# Wi-Fi access-point credentials for the Pico W web interface.
#
# Copy this file to `secrets.py` (which is git-ignored) and set your own
# SSID/password before copying it to the Pico. Only the Pico W variant uses
# this; the standard Pico ignores it.

secrets = {'ssid': 'CHANGE_ME_SSID', 'password': 'CHANGE_ME_PASSWORD'}
